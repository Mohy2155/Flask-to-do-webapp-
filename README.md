# Flask RESTful API: Task Manager

A simple RESTful API built with Flask to manage tasks. Supports CRUD operations.

## Features

- GET /tasks
- GET /tasks/<id>
- POST /tasks
- PUT /tasks/<id>
- DELETE /tasks/<id>

## How to Run

```bash
pip install -r requirements.txt
python app.py
```
